import { SafeLabelPipe } from './safe-label-pipe';

describe('SafeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SafeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
