name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger deploy (placeholder)
        run: echo "Deployment handled by hosting platform (Vercel/Render)"