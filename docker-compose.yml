version: '3.9'

services:
  backend:
    build:
      context: .
      dockerfile: Backend/Dockerfile
    ports:
      - "3000:3000"

  frontend:
    build:
      context: ./portfolio
      dockerfile: Dockerfile
    ports:
      - "8080:80"